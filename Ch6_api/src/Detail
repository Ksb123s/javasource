
자바가 제공해주는 클래스 사용법

java.lang * = > 자바가 제공하는 패키지 전부 사용



 컬렉션 프레임워크
  -데이터 군을 저장하는 클래스들을 표준화한 설계
 
 1) Collection
 상속 인터페이스)-List
                 1.순서가 있는 테이터의 집합(테이터의 중복 허용)
                 2. ArrayList, LinkedList,Stack,Vector ..
            ArrayList

            LinkedList
                -   ArrayList, Vector 비순차적인 데이터의 추가 또는 삭제에 시간이 많이 걸리는 부분을 보완
            Stack

            Queue


(상속 인터페이스)     -Set
            HashSet

            TreeSet
                -이진검색(탐색)트리 : 정렬, 검색, 범위검색에 높은 성능을 보이는 자료구조
                -모든 노드는 최대 두 개의 자식노드를 가짐
                -왼쪽 자식 노드의 값은 부모노드의 값보다 작고 오른쪽 노드의 값을 부모 노드의 값보다 커야 한다.
                -노드의 추가,삭제에 시간이 걸린다
                -검색과 정렬에 유리
                -증복된 값을 저장하지 못함
(비상속 인터페이스)   -Map 

